type Mag @model @auth(rules: [{allow: public}]) {
  id: ID!
  image_url: String
  tag: String
  tag_color: String
  text: String
}

type G22 @model @auth(rules: [{allow: public}]) {
  id: ID!
  image_url: String
}
 